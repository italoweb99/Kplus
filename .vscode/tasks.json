{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "server",
            "type": "npm",
            "script": "start",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "label": "front",
            "type": "npm",
            "script": "dev",
            "options": {
                "cwd":"${workspaceFolder}/kplus-web"
            }
        },
        {
            "label":"start site",
            "dependsOn": ["server","front"]
        }
    ]
}